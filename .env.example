# ===========================================
# GameZone Portal - Konfiguracja środowiskowa
# ===========================================
# Skopiuj ten plik jako .env i uzupełnij wartości

# Port serwera
PORT=3000

# Sekret sesji (wygeneruj losowy ciąg znaków)
# Możesz użyć: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-secret-key-change-this-in-production

# ===========================================
# Steam Web API (opcjonalne)
# ===========================================
# Aby włączyć logowanie Steam:
# 1. Przejdź na https://steamcommunity.com/dev/apikey
# 2. Zaloguj się na swoje konto Steam
# 3. Wpisz nazwę domeny (np. localhost dla testów)
# 4. Skopiuj wygenerowany klucz poniżej

STEAM_API_KEY=
STEAM_REALM=http://localhost:3000
STEAM_RETURN_URL=http://localhost:3000/api/auth/steam/callback
